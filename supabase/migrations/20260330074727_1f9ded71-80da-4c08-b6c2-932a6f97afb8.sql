
-- Enable leaked password protection (HaveIBeenPwned check)
-- This is done via auth config, but we can also move the extension out of public schema
-- For now, just documenting that HIBP is enabled via configure_auth
SELECT 1;
